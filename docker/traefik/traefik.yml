entryPoints:
  http:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: "whatsapp_sender_public"

api:
  dashboard: true

log:
  level: DEBUG
